import ProductCard from "@/components/shop/ProductCard";import {products} from "@/data/products";export default function Laptops(){return <section className="section"><div className="container"><span className="eyebrow">LAPTOPS</span><h1 className="section-title">Power, Anywhere.</h1><p className="section-copy">Gaming machines built for demanding work and play.</p><div className="product-grid" style={{marginTop:40}}>{products.filter(p=>p.category==="laptops").map(p=><ProductCard key={p.id} product={p}/>)}</div></div></section>}