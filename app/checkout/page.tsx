"use client";import Link from "next/link";import {useCart} from "@/components/cart/CartProvider";export default function Checkout(){const{items,subtotal}=useCart();const count=items.reduce((sum,item)=>sum+item.quantity,0);return <section className="section"><div className="container"><span className="eyebrow">CHECKOUT</span><h1 className="section-title">Complete Your Order</h1><div className="summary" style={{maxWidth:600,marginTop:35}}><p style={{color:"var(--muted)",lineHeight:1.7}}>Your cart is saved in this browser. Payment integration will be connected after the storefront migration is verified.</p><div className="summary-row"><span>Items</span><strong>{count}</strong></div><div className="summary-row"><span>Total</span><strong>{"$"+subtotal.toFixed(2)}</strong></div>{items.length?<button className="button button-primary" style={{marginTop:15}}>Continue to Payment</button>:<Link className="button button-primary" href="/shop" style={{marginTop:15}}>Return to Shop</Link>}</div></div></section>}